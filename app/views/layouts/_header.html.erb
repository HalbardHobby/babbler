<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to 'Babbler', root_path, class: "navbar-brand" %>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <form class="d-flex">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">  
          <% if user_signed_in? %>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= current_user.nickname %>
              </a>
              <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                <li><%= link_to 'Edit Profile', edit_user_registration_path, 
                    class: "nav-link", method: :get, class: "dropdown-item" %></li>
                <li><%= link_to 'Sign Out', destroy_user_session_path, 
                    class: "nav-link", method: :delete, class: "dropdown-item" %></li>
              </ul>
            </li>
          <% else %>
            <%= link_to "Sign in", new_user_session_path,
            :class => "btn btn-outline-success", :method => :get %>
            <%= link_to "Sign up", new_user_registration_path, 
            :class => "btn btn-outline-secondary", :method => :get %>
          <% end %>
        </ul>
      </form>
    </div>
  </div>
</nav>